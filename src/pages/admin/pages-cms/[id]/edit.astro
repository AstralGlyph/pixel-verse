---
/**
 * @fileoverview 后台页面编辑（SSR）
 * @description 渲染 React 页面编辑器组件，受认证中间件保护
 */

import { PageEditor } from '../../../../admin/pages/pages-editor';
import AdminLayout from '../../../../layouts/AdminLayout.astro';

const { id } = Astro.params;

---

<AdminLayout title="编辑页面">
  <PageEditor client:load pageId={id} />
</AdminLayout>
